# Build
bin/
obj/
*.user
*.suo
*.cache

# User-specific config (niemals echte Secrets committen)
appsettings.*.local.json
**/appsettings.*.local.json
*.secrets.json

# User Secrets (werden vom Tool verwaltet)
# Die eigentlichen Secrets liegen außerhalb des Repos

# IDE / Editor
.vs/
.vscode/
*.rsuser

# OS
.DS_Store
Thumbs.db

# Secrets / lokale Konfiguration (Harness security.md §8)
.env
*.local
secrets.json
appsettings.Local.json
**/appsettings.Local.json
*.received.*

# Test- und Analyseberichte
TestResults/
*.trx
coverage*.xml
